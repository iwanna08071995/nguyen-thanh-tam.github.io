const LIST_LANGUAGE = ["en", "vi"];
const LANGUAGES = {
  EN: "en",
  VI: "vi",
};

const MODE_DARK_LIGHT = {
  DARK: true,
  LIGHT: false,
};

const SIZE_ICON_DEFAULT = "1.6rem";

const SIZE_DIVIDER_DEFAULT = "100%";

export {
  LIST_LANGUAGE,
  MODE_DARK_LIGHT,
  SIZE_ICON_DEFAULT,
  SIZE_DIVIDER_DEFAULT,
  LANGUAGES,
};
